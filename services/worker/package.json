{
  "name": "@meops/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@meops/core": "workspace:*",
    "@meops/content": "workspace:*",
    "@meops/extraction": "workspace:*",
    "@meops/store": "workspace:*"
  },
  "scripts": {
    "build": "MEOPS_REPO_PATH=../.. MEOPS_STORE_PATH=../../.meops/signals.json tsc -p tsconfig.json",
    "dev": "MEOPS_REPO_PATH=../.. MEOPS_STORE_PATH=../../.meops/signals.json tsc -w -p tsconfig.json",
    "lint": "echo 'no lint configured yet'",
    "test": "echo 'no tests configured yet'",
    "clean": "rm -rf dist"
  }
}
