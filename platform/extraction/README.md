# Extraction

Git-history signal extraction lives here.

This package reads recent commits from a repository, classifies them into meops
signal types, and returns normalized signal candidates for the worker to store.
